{% extends 'KitpagesCmsBundle:Page:layout.html.twig' %}

{% block kitpages_cms_body %}
<div class="layout-inside-column">
    <div class="app-block-contextual">
        {{ render(controller('KitpagesCmsBundle:Block:widget', {'slug': app.request.locale ~ '-contact-contextual'} )) }}
    </div>

</div>
<div class="layout-inside-main">
    <h1>Nous contacter</h1>
    {{ render(controller('KitpagesCmsBundle:Zone:widget', {'slug': app.request.locale ~ '-contact-intro'} )) }}
    {{ render(controller('KitpagesCmsBundle:Zone:widget', {'slug': app.request.locale ~ '-contact-body'} )) }}
</div>
<div style="clear:both;"></div>

{% endblock kitpages_cms_body %}
